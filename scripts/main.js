document.getElementById('form-cadastro').addEventListener('submit', function (event) {
  event.preventDefault();

  // Os alunos devem escrever o código a partir daqui.
  // 1. Capturar valores dos inputs
  // 2. Tratar os dados (trim, toLowerCase, split, verificar "música")
  // 3. Exibir resultado formatado na <section id="resultado">
  // 4. Exibir também o objeto formatado no console.log()
});
